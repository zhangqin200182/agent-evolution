---
id: traj-20260503-023847-steps
target_section: steps
action: append
description: "数据完整性检查: perf_stats 缺失和 reward 不完整时输出警告"
status: proposed
source: trajectory-analysis
source_sessions: ["490054f5-e0e8-4813-827f-670c72590443"]
---

### 数据完整性检查

在 Step 2 提取训练详情后，检查数据完整性:

- 如果 perf_stats 为空:
  输出警告并在报告中标注: 性能分析数据缺失

- 如果 reward_trend 数据点 < total_steps * 0.5:
  输出警告并在报告中标注: reward 趋势为部分数据

数据缺失不阻断分析流程，但必须在报告中明确标注，避免基于不完整数据做出错误结论。
